interface:
  display_name: "Skill Safety Audit"
  short_description: "Audit Agent Skills before installing them with static v1.1 checks"
  default_prompt: "Use $skill-safety-audit to audit this Agent Skill before installation and report SAFE, REVIEW, or DANGEROUS."
policy:
  allow_implicit_invocation: true
